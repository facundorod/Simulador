import { ScenarioTextPipe } from './scenario-text.pipe';

describe('ScenarioTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ScenarioTextPipe();
    expect(pipe).toBeTruthy();
  });
});
