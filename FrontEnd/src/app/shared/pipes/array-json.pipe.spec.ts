import { ArrayJsonPipe } from './array-json.pipe';

describe('ArrayJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
